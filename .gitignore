# clasp credentials — a live token that can rewrite the backend. This repo is PUBLIC.
.clasprc.json
.clasp.json

# Local deploy config (holds the Apps Script ID / deployment ID)
deploy.config.json

node_modules/
